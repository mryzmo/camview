* add multi-profile (find data first)
* SOR viewer
* make it so background actually is background (add toggle for alt, make it go every 2nd)
* add LV sync
* give possibility to change "profile ramp" avg
